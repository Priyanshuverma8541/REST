<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1 class="quora">Quora Posts</h1>
   <% for( post of posts ) { %>
        <div class="post">
            <h3 class="username">@<%= post.username %></h3>
            <h4><%= post.details %></h4>
            <a href="http://localhost:8000/posts/<%= post.id %>">Show details</a>
            <a href="http://localhost:8000/posts/<%= post.id %>/edit">Edit details</a>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>Delete Post</button>
            </form>
        </div>
   <% } %>

   <br><br>
   <a href="http://localhost:8000/posts/new">Create new post</a>
</body>
</html>
